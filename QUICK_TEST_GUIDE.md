# Castle 간단 테스트 가이드

이 문서는 개발 지식이 없는 사용자가 PC #2와 Android 휴대폰에서 Castle의 기본 기능만 확인하기 위한 안내서입니다.

예상 소요 시간은 약 10분입니다.

## 준비물

- PC #2와 Android 휴대폰
- 두 기기가 연결된 같은 가정용 네트워크
- PC #2에 설치된 Node.js LTS
- PC #2에 복사된 Castle 프로그램 폴더
- 미디어가 들어 있는 PC #2의 실제 로컬 `castle` 폴더

`castle` 경로는 `D:\...\castle`처럼 드라이브 문자로 시작해야 합니다. `\\DESKTOP-VHU9KUS\...`처럼 시작하는 네트워크 경로는 사용하지 마세요.

## 1. 서버 시작

1. PC #2의 파일 탐색기에서 Castle 프로그램 폴더를 엽니다. 이 폴더에는 `start-server.ps1` 파일이 있어야 합니다.
2. 파일 탐색기의 주소 표시줄을 클릭하고 `powershell`을 입력한 다음 Enter를 누릅니다.
3. 아래 명령에서 예시 경로를 실제 로컬 `castle` 경로로 바꿔 입력합니다.

   ```powershell
   powershell -ExecutionPolicy Bypass -File .\start-server.ps1 -CastleRoot 'D:\실제\경로\castle' -Port 8080
   ```

   실제 경로를 모르면 PC #2의 파일 탐색기에서 미디어가 들어 있는 `castle` 폴더를 직접 연 뒤 주소 표시줄을 클릭해 표시되는 경로를 복사하세요. 복사한 경로가 `\\`로 시작하면 로컬 경로가 아니므로 설치 담당자에게 실제 드라이브 경로를 확인해 달라고 요청하세요.

4. 다음과 비슷한 내용이 나타나면 서버가 정상적으로 시작된 것입니다.

   ```text
   Castle root: D:\실제\경로\castle
   Listening: http://0.0.0.0:8080
   LAN URL: http://192.168.219.104:8080
   ```

PowerShell 창은 테스트가 끝날 때까지 닫지 마세요.

## 2. PC #2에서 먼저 확인

PC #2의 웹 브라우저 주소창에 다음 주소를 입력합니다.

```text
http://127.0.0.1:8080
```

다음을 확인하세요.

- Castle 화면이 열린다.
- `castle` 바로 아래의 폴더가 메뉴로 보인다.
- 메뉴를 누르면 그 안의 폴더가 카드로 보인다.

여기서 화면이 열리지 않으면 Android 테스트로 넘어가지 말고 아래의 “문제가 생겼을 때”를 확인하세요.

## 3. Android 휴대폰에서 확인

1. 휴대폰이 PC #2와 같은 Wi-Fi에 연결되어 있는지 확인합니다.
2. 휴대폰 브라우저를 엽니다.
3. 서버 시작 화면에 표시된 `LAN URL`을 주소창에 입력합니다. 현재 예정된 주소는 다음과 같습니다.

   ```text
   http://192.168.219.104:8080
   ```

4. 아래 항목을 차례로 확인합니다.

### 최소 확인 목록

- [ ] 첫 화면에 메뉴 폴더가 보인다.
- [ ] 메뉴를 누르면 미디어 카드가 보인다.
- [ ] 카드를 누르면 폴더 안의 파일 목록이 보인다.
- [ ] 하위 폴더가 있으면 열 수 있고, 위쪽 경로 표시로 돌아갈 수 있다.
- [ ] 이미지 파일을 누르면 원본 이미지가 열린다.
- [ ] 오디오 파일을 누르면 재생되고, 일시정지와 위치 이동이 된다.
- [ ] 비디오 파일을 누르면 재생되고, 재생 위치를 앞으로 이동할 수 있다.
- [ ] 뒤로 가기와 홈 이동이 정상적으로 작동한다.
- [ ] 화면의 글자와 버튼이 휴대폰에서 누르기 어렵지 않다.

특정 비디오가 열리지만 재생되지 않는 경우에는 휴대폰 브라우저가 해당 영상 코덱을 지원하지 않는 것일 수 있습니다. 다른 MP4 영상도 한 번 확인해 주세요.

## 4. 테스트 종료

서버를 실행한 PowerShell 창을 선택하고 `Ctrl+C`를 누릅니다.

창이 명령 입력 상태로 돌아오면 서버가 종료된 것입니다. 브라우저를 새로고침했을 때 Castle 화면이 더 이상 열리지 않는 것도 확인할 수 있습니다.

## 문제가 생겼을 때

### `Node.js is not installed`라고 표시되는 경우

PC #2에 Node.js LTS가 설치되지 않았거나, 설치 후 PowerShell을 다시 열지 않은 상태입니다. Node.js LTS를 설치한 뒤 모든 PowerShell 창을 닫고 다시 시작하세요.

### `CastleRoot` 오류가 표시되는 경우

- 입력한 폴더가 실제로 존재하는지 확인하세요.
- 경로 양쪽의 작은따옴표를 지우지 마세요.
- `\\`로 시작하는 네트워크 경로가 아니라 PC #2의 실제 로컬 경로를 사용하세요.

### PC에서는 열리지만 휴대폰에서는 열리지 않는 경우

- 휴대폰과 PC #2가 같은 공유기에 연결되어 있는지 확인하세요.
- 서버 PowerShell 창이 계속 열려 있는지 확인하세요.
- 주소와 포트가 서버 화면에 표시된 `LAN URL`과 같은지 확인하세요.
- Windows 방화벽의 `Castle LAN Media Browser` 인바운드 규칙이 적용되었는지 설치 담당자에게 확인해 달라고 요청하세요.

### 메뉴가 하나도 보이지 않는 경우

서버를 시작할 때 지정한 `castle` 폴더 바로 아래에 메뉴용 폴더가 있는지 확인하세요. 파일만 있고 폴더가 없으면 첫 화면에 메뉴가 나타나지 않습니다.

## 결과를 전달하는 방법

문제가 있다면 아래 양식을 복사해 작성하고, 가능하면 오류 화면 사진도 함께 전달하세요.

```text
테스트 날짜:
휴대폰 기종:
사용한 브라우저:
Castle root 경로:
사용한 LAN URL:

PC에서 열림: 성공 / 실패
휴대폰에서 열림: 성공 / 실패
메뉴와 카드: 성공 / 실패
이미지: 성공 / 실패
오디오: 성공 / 실패
비디오 재생과 위치 이동: 성공 / 실패
하위 폴더 탐색: 성공 / 실패

문제가 발생한 단계:
화면에 표시된 오류 문구:
문제가 발생한 파일 이름:
```
