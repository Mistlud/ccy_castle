{
  "name": "ccy-castle",
  "version": "0.1.0",
  "private": true,
  "description": "LAN-only, read-only media browser for a configured castle directory",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node test/path-safety.test.js && node test/library.test.js && node test/server.test.js && node test/thumbnails.test.js",
    "check": "node --check server.js && node --check src/path-safety.js && node --check src/media-types.js && node --check src/library.js && node --check src/thumbnails.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
